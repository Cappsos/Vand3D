TODO
====

1. Update `test.py` to compute dynamic padded depth rather than using the fixed
   value (`padded_depth = 160`). See the comment around line 359.

2. Improve documentation around dataset preparation, including steps for
   generating the `meta.json` and expected directory structure.

3. Provide a script or instructions to automatically download and preprocess
   the BraTS dataset so that new users can reproduce experiments more easily.

4. Consolidate configuration files and clarify the purpose of each YAML file in
   `configs/`—for example, which settings apply to 2D vs 3D experiments.

5. Consider adding unit tests for utility functions in `utils/` to ensure
   transformations, metrics, and postprocessing steps behave as intended.
